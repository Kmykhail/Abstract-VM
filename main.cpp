#include "AbstractVM.hpp"

int main()
{
    /*Virt_Machine::A_vm *some = new Virt_Machine::A_vm;
    IOperand * one = new Operand<int8_t >(10, Int8 ,"10");
    //some->_vec.push_back(one);
    some->getVector().push_back(one);
    std::cout << some->getVector().size() << std::endl;*/
    AbstractVM vm;
    return 0;
}